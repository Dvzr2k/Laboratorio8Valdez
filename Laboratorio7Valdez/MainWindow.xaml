<Window x:Class="Laboratorio7Valdez.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="0,273,0,0">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        

        <TextBox x:Name="txtName" Margin="10" Grid.Row="0" Width="200" />
        <Button Content="Buscar" Click="Button_Click" Margin="10" Grid.Row="1" Width="100" />
        <Button Content="Crear" Click="CreateButton_Click" Margin="350,-142,350,160" Width="100" />
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="True" Margin="10" Grid.Row="2" />
        <TextBox x:Name="txtName1" Margin="300,-248,300,268" Grid.Row="0" Width="200" />
        <TextBox x:Name="txtPrice" Margin="300,-215,300,235" Grid.Row="0" Width="200" />
        <TextBox x:Name="txtStock" Margin="300,-181,300,201" Grid.Row="0" Width="200" />
        
    </Grid>
</Window>